{
  "builders": [{
    "iso_checksum_type": "sha1",
    "iso_checksum_url": "{{ user `mirror` }}{{ user `release` }}/sha1sums.txt",
    "iso_url": "{{ user `mirror` }}{{ user `release` }}/archlinux-{{ user `release` }}-dual.iso",
    "ssh_username": "root",
    "type": "qemu"
  }],
  "variables": {
    "mirror": "https://mirrors.kernel.org/archlinux/iso/",
    "release": "2016.10.01"
  }
}
